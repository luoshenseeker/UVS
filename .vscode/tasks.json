{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "buildcpp",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",
                "${file}",
                "-o",
                "build"
            ]
        },
        {
            "label": "buildc",
            "type": "shell",
            "command":"gcc",
            "args": [
                "-g",
                "${file}",
                "-o",
                "build"
            ]
        }
    ]
}